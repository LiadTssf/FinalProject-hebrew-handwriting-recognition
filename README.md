## לוח זמנים מוצע - פרויקט זיהוי כתב יד בעברית (8 שבועות)

| שבוע | פוקוס מרכזי                   | משימות ומטרות עיקריות                                                                                                                                                                                                                                                          | תוצרים/אבני דרך                                          | הערות                                                                                                                               |
| :--- | :------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **סגירת גישה והכנה**          | - **החלטה סופית:** אישור גישת `Object Detection` (`YOLO`). <br> - **הכנת סביבה:** `Colab` מוכן, כלי אנוטציה (`LabelImg`/`Roboflow` export) עובד, `classes.txt` סופי.<br> - **אנוטציית פיילוט:** סימון מלא של 5-10 תמונות מגוונות (`Bounding Box` + `Label`).<br> - **בדיקת `Pipeline`:** הרצת סקריפט אימון קצר (`Test Training`) לוודא שהקוד רץ מקצה לקצה (להתעלם מדיוק).<br> - **פגישה עם המנחה:** דיון בגישה שנבחרה ותוכנית ראשונית. | קוד בסיסי עובד (`Data Load` -> `Train` -> `Save`).<br> דאטהסט ראשוני קטן מסומן. | עדיפות גבוהה: להריץ את שלד הקוד. לפתור בעיות התקנה מיידית.                                                                      |
| **2** | **בניית דאטהסט ומודל בסיסי** | - **אנוטציה אינטנסיבית:** סימון כמות משמעותית (שאיפה ל-50-100+ תמונות סה"כ). דגש על איכות ועקביות.<br> - **חלוקת `Train/Validation`:** יצירת סטים נפרדים לאימון וולידציה.<br> - **אימון משמעותי ראשון:** אימון `YOLO` (למשל `yolov8n/s`) על הדאטהסט הראשוני ליותר `Epochs` (50-100). שאיפה ליכולת זיהוי בסיסית.<br> - **קוד מיון ופלט בסיסי:** פיתוח סקריפט `Python` לטעינת המודל, הרצת `Inference`, מיון תוצאות (`Y` ואז `X` מימין לשמאל - `RTL`), והפקת מחרוזת טקסט גולמית. | מודל מאומן `v0.1` (`.pt`).<br> סקריפט `Python` בסיסי ל-`Inference` ומיון.<br> ~100 תמונות מסומנות. | **מטרה: מערכת בסיסית עובדת מקצה לקצה**, גם אם הדיוק נמוך. הוכחת היתכנות (`Proof of Concept`).                                        |
| **3** | **אינטגרציה לאתר ושיפורים**   | - **אתר בסיסי:** וידוא שמבנה האתר (העלאה, אזור תצוגה) פונקציונלי.<br> - **אינטגרציית `Backend`:** חיבור סקריפט ה-`Inference`/מיון (משבוע 2) ל-`Backend` של האתר (למשל `Flask`/`Django`). העלאת תמונה -> הרצת מודל -> הצגת טקסט ממוין.<br> - **הערכת מודל `v0.1`:** ניתוח כשלים, זיהוי שגיאות נפוצות.<br> - **המשך אנוטציה:** הוספת תמונות בדגש על סוגים שהמודל התקשה איתם. | אב-טיפוס בסיסי של האתר עובד.<br> מודל משולב ב-`Backend`.<br> עוד תמונות מסומנות.     | פוקוס על חיבור החלקים. לא לדאוג יותר מדי לדיוק מושלם *עדיין*.                                                                   |
| **4** | **שיפור מודל ופיצ'רים**      | - **הרחבת דאטהסט:** המשך אנוטציה (שאיפה ל-150-200+ תמונות אם אפשר).<br> - **אימון מחדש `v0.2`:** אימון עם דאטהסט גדול יותר, אולי ניסוי עם מודל `YOLO` מעט גדול יותר (`yolov8m`) או יותר `Epochs`.<br> - **שיפור מיון/ריווח:** ליטוש לוגיקת ה-`Post-processing` לטיפול טוב יותר במעברי שורה וריווח בין מילים.<br> - **קונספט `User Adaptation`:** *תכנון* טכני של התאמה אישית/`Fine-tuning` (ממשק העלאה, הפעלת סקריפט `Fine-tuning`). *התחלת קידוד* בסיסי אם הזמן מאפשר. | מודל משופר `v0.2` (`.pt`).<br> לוגיקת מיון/ריווח משופרת.<br> תכנון `User Adaptation` סופי. | הדיוק אמור להיות טוב יותר משמעותית. `Adaptation` יכול להיות רק תוכנית/קוד בסיסי בשלב זה.                                |
| **5** | **תיעוד - חלק 1**             | - **התחלת ספר פרויקט (`Project Book`):** כתיבת טיוטות ראשונות ל-`General Description`, `Solution Description` (ארכיטקטורה, רציונל בחירת `YOLO`, לוגיקת מיון), `Development Process`, `Tools Used`.<br> - **התחלת מדריך למשתמש (`User Guide`):** טיוטת הוראות בסיסיות לשימוש באתר (העלאה, צפייה בתוצאות).<br> - **בדיקות:** ביצוע בדיקות פונקציונליות בסיסיות לאתר ולמודל. תיקון באגים ברורים. | טיוטות חלקי `Project Book` ו-`User Guide`.<br> דוח בדיקות בסיסי / רשימת באגים.        | מעבר פוקוס קל לתיעוד בזמן שהמודל אולי מתאמן ברקע/בלילה.                                                                       |
| **6** | **תיעוד - חלק 2 ווידאו**       | - **שיפור ספר פרויקט:** פירוט `Challenges & Solutions`, `Results & Conclusions`, `Lessons Learned`, `Project Metrics`.<br> - **השלמת מדריך למשתמש:** הוספת צילומי מסך, כיסוי תרחישי שימוש עיקריים.<br> - **התחלת מדריך למתחזק (`Maintenance Guide`):** תיעוד סביבת הרצה, תלויות, הוראות הרצה/אימון מחדש.<br> - **יצירת תסריט ו-`Storyboard` לווידאו:** תכנון סרטון 2 הדקות המדגים את התוכנה. | טיוטות כמעט סופיות לתיעוד.<br> `Storyboard`/תסריט לווידאו.                            | תיעוד לוקח זמן – להקצות מאמץ משמעותי השבוע.                                                                                |
| **7** | **פוסטר/מצגת וליטושים**      | - **יצירת פוסטר/מצגת:** עיצוב לפי הדרישות (אנגלית, סעיפים עיקריים). פוקוס על בהירות וויזואליות.<br> - **הקלטה ועריכת וידאו:** צילום מסך, הוספת קריינות/טקסט, עריכה לאורך מקסימלי של 2 דקות.<br> - **אימון סופי למודל (אופציונלי):** הרצת אימון אחרונה אם נוסף דאטה משמעותי או זוהו שיפורים גדולים.<br> - **בדיקות סופיות ותיקוני באגים:** בדיקה יסודית של כל המערכת. תיקון באגים קריטיים שנותרו.<br> - **הכנת דמו:** וידוא הדגמה חיה חלקה / הרצת וידאו תקינה למצגת. | פוסטר/שקפי מצגת סופיים.<br> קובץ וידאו סופי.<br> מודל סופי `v1.0` (`.pt`).               | תרגול המצגת/דמו. וידוא שכל המרכיבים (קוד, תיעוד, ויזואליה) מחוברים היטב.                                                   |
| **8** | **הכנה להגשה סופית**         | - **סיום תיעוד:** הגהה לכל המסמכים (`Project Book`, `User Guide`, `Maintenance Guide`). וידוא פורמט תקין.<br> - **ניקוי קוד ו-`Readme`:** הוספת הערות לקוד, וידוא ש-`requirements.txt` מדויק, כתיבת `README.md` ברור עם הוראות התקנה/הרצה לריפוזיטורי ה-`GIT`.<br> - **אריזת הגשה:** ארגון כל הקבצים (קוד, משקולות `.pt`, קבצי PDF לתיעוד, פוסטר/PPT, וידאו MP4) במבנה הנדרש ב-`GIT`.<br> - **הגשה!** | תיעוד סופי ומלוטש.<br> ריפוזיטורי `GIT` נקי.<br> חבילת הגשה מלאה.                  | בדיקה כפולה של כל דרישות ההגשה. גיבוי של הכל. להירגע (קצת)!                                                               |

































<<<<<<< HEAD
# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript and enable type-aware lint rules. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.
=======
# FinalProject-hebrew-handwriting-recognition
The focus is on achieving precise recognition through contextual understanding, enabling seamless digitization and editing of Hebrew documents.
>>>>>>> 328ec67017b9c95a228b7305935bf2cfed5a0f2c
